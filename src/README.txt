JAVA implementation of sudoku solver

Make sure all dependencies are downloaded, it will prompt you to download if you do not (JUnit for tests, etc)
Input puzzle shown before and after it is solved after running. You can change size of boards and test it with different dimensions by simply changing the .txt files.
